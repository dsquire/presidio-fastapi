# Example environment configuration file
# Copy to .env and adjust settings for your environment

# NLP Engine Configuration
NLP_ENGINE_NAME=spacy
SPACY_MODEL_EN=en_core_web_lg

# API Configuration
MAX_TEXT_LENGTH=102400
MIN_CONFIDENCE_SCORE=0.5

# Security Settings
# CORS Allowed Origins - comma-separated list of allowed origins
# Examples:
# Development (specific origins): ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
# Production (specific domains): ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
# 
# NOTE: Using "*" is not recommended for production environments as it allows any origin
# ALLOWED_ORIGINS=*
ALLOWED_ORIGINS=http://localhost:3000,https://app.example.com
